import {createContext} from 'react';

 export const DomainContext = createContext<string>("https://server.lifewaygen.ga/");
//  export const DomainContext = createContext<string>("http://localhost:8000");
